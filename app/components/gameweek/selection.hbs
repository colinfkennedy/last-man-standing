<div class="flex flex-col flex-grow rounded-xl  w-full shadow lg:max-w-2xl border p-4 space-y-4 bg-{{if this.hasSelection 'white' 'charcoalRed'}}">
  <div class="flex flex-row items-center">
    <Babber @babber={{@babber}}/>
    {{#if this.hasSelection}}
      <Club @club={{this.clubSelection}} class="text-right flex-row-reverse -mr-2" />
    {{/if}}
  </div>
</div>
